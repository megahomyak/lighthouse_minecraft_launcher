"Lighthouse" is a small CLI Minecraft Launcher intended for tinkerers. It has no dependencies outside of Python's standard library.

USAGE:
* Go to the directory you'd like to use for storing launcher data
* `.../lighthouse.py list` lists versions
* `.../lighthouse.py ensure VERSION` ensures that all files for VERSION are downloaded
* `.../lighthouse.py run VERSION` runs VERSION
* `.../lighthouse.py run VERSION` detaches VERSION
